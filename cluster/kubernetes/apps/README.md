# Kubernetes Apps

Workloads and namespaces deployed via Flux. Each subfolder should own its namespace, Kustomization, and app-specific manifests or HelmReleases. Keep secrets SOPS-encrypted alongside the app.
