# Grafana dashboards
Drop ConfigMaps with the `grafana_dashboard: "1"` label here so the Grafana sidecar picks them up.
The `k3s-overview` dashboard already lives in this directory as a baseline for cluster health.
