# Observability Otel Collector

This directory exists to satisfy the v7 convergence claim for the observability otel-collector path. It reuses the concrete resources defined under `../monitoring/otel-collector` via the `kustomization.yaml` resource reference.
