# Certificate helpers
Place self-managed CertificateRequests, Secrets, or cert-manager resources here if you need supplemental TLS assets.

Right now the Cloudflare Tunnel owns the global certificates for Grafana, Prometheus, etc., but this directory keeps a home for future CA-managed rollouts.
