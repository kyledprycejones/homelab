# Kubernetes Platform

Cluster-wide services and day-0/day-1 components (ingress, storage, monitoring, logging, DNS, certs, security). Each concern has its own folder with Kustomizations for Flux to reconcile.
